name: SimpleReconnectServerSide
version: ${version}
main: com.simpleplugins.reconnect.serverside.SimpleReconnectServerSidePlugin
api-version: "1.20"
authors: [ "sardidefcon", "TacxTV" ]
description: Sends player death events to Velocity for cross-server respawn handling.
